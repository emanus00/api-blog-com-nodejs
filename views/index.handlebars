<div class="bg-light p-5 rounded-lg m-3">
  <h1 class="display-4">Bem vindo ao meu blog!</h1>
  <p class="lead">Este blog foi feito 100% pelo Node.JS + MongoDB + Express, eu, Emanuel com a ajuda de um professor criei este blog. Aproveite!</p>
  <hr class="my-4">
  <p>Aqui você verá tudo sobre ciência da computação.</p>
  {{#if user}}
  {{else}}
  <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
  {{/if}}
</div>

<hr>

<h2>Postagens recentes: </h2>
<hr>

{{#each postagens}}
    <div class="card">
        <div class="card-body">
            <small>Categoria: <strong>{{categoria.nome}}</strong></small>
            <br>
            <h3>{{titulo}}</h3>
            <p>{{descricao}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia Mais</button></a>
        </div>
    </div>
{{else}}
    <h5>Nenhuma postagem registrada.</h5>
{{/each}}